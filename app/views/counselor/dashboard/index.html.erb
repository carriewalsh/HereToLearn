<div class="date-container">
  <h2 class="date"><%= date %></h2>
</div>
<br>
<div class="stats-header">
  <h2><%= link_to "View machine accuracy information", counselor_machine_path, class:'form-button' %></h2>
</div>
<br>
<div class="banner">
  <div class="dynamic-container">
    <div class="dynamic">
      <section class='flagged-strategies'>
        <h3>Strategies we've flagged as "potentially unhelpful":</h3>
        <% facade.flagged_strats.each do |strat| %>
        <section class='strategy'>
          <p><%= strat.strategy %></p>
          <%= button_to 'Delete this strategy', deactivate_strategy_path(strat), method: :patch %>
          <%= button_to "Approve this strategy", approve_strategy_path(strat), method: :patch %>
        </section>
        <% end %>
      </section>
    </div>

  </div>
</div>

<div class="banner">
  <div class="dynamic-container">
    <div class="dynamic">
      <div class="text-container">
        <h3 class="grey-heading">9th Grade</h3>
        <div class="student-container">
          <% facade.students_9.each do |student| %>
          <div class="student-card" id=<%= student.id %>>
            <%= link_to "#{student.first_name} #{student.last_name}", counselor_student_path(student.id), class:"small-link" %>
            <div class="smaller-grey">
              #<%= student.student_id %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="dynamic-container">
    <div class="dynamic">
      <div class="text-container">
        <h3 class="grey-heading">10th Grade</h3>
        <div class="student-container">
          <% facade.students_10.each do |student| %>
          <div class="student-card" id=<%= student.id %>>
            <%= link_to "#{student.first_name} #{student.last_name}", counselor_student_path(student.id), class:"small-link" %>
            <div class="smaller-grey">
              #<%= student.student_id %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>


<div class="banner">
  <div class="dynamic-container">
    <div class="dynamic">
      <div class="text-container">
        <h3 class="grey-heading">11th Grade</h3>
        <div class="student-container">
          <% facade.students_11.each do |student| %>
          <div class="student-card" id=<%= student.id %>>
            <%= link_to "#{student.first_name} #{student.last_name}", counselor_student_path(student.id), class:"small-link" %>
            <div class="smaller-grey">
              #<%= student.student_id %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div class="dynamic-container">
    <div class="dynamic">
      <div class="text-container">
        <h3 class="grey-heading">12th Grade</h3>
        <div class="student-container">
          <% facade.students_12.each do |student| %>
          <div class="student-card" id=<%= student.id %>>
            <%= link_to "#{student.first_name} #{student.last_name}", counselor_student_path(student.id), class:"small-link" %>
            <div class="smaller-grey">
              #<%= student.student_id %>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
